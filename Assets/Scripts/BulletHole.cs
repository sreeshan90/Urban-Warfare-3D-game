using UnityEngine;
using System.Collections;

public class BulletHole : MonoBehaviour {

	GameObject bulletHole;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {

			
			if(gameObject.name == "BulletHole(Clone)"){
			//Destroy(gameObject, 10.0f);
			}


	}
}
